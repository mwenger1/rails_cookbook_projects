<!DOCTYPE html>
<html>
<head>
  <title>Eventstr</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <style>
  .jumbotron {
    margin-top: 30px;
    }
   .navbar-brand {
    float: left;
    margin-left: -15px;
    margin-right: 5px;
    margin-top: -11px;
    }
   .navbar-nav > li > a {
  border-bottom-left-radius:4px;
  border-bottom-right-radius:4px;
  border-top-left-radius:4px;
  border-top-right-radius:4px;
  color:#777777;
  line-height:29px;
  padding-bottom:15px;
  padding-top:15px;
  }
  </style>
  <%= csrf_meta_tags %>
</head>
<body>
    <!-- Static navbar -->
    <div class="navbar navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/"><%= image_tag 'eventstr.png'%></a>
        <div class="nav-collapse collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Events", events_path %></li>
            <% if user_signed_in? %>
             <li class="active"><%= link_to "My Events", my_events_path %></li>
            <%end%>
          </ul>
          <ul class="nav navbar-nav pull-right">
           <% if user_signed_in? %>
            <li><%=link_to "#{current_user.email}" %></li>
            <li class="active"><%= link_to "Logout", destroy_user_session_path, :method => :delete%></li>
           <%else%>
            <li><%= link_to "Login", new_user_session_path %></li>
            <li class="active"><%= link_to "SignUp", new_user_registration_path%></li>
           <%end%>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
      

    <div class="container">
      <%= yield %>


    </div> 


</body>
</html>
